allprojects {


    repositories {
        mavenLocal()
        jcenter()

    }

    configurations.all {
        exclude group: 'com.sun.xml.bind'
    }

    task wrapper(type: Wrapper) {
        gradleVersion = gradle_version
    }

}
